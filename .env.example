# DeepSeek API（必填，仅 generate 需要）
DEEPSEEK_API_KEY=sk-your-key-here

# 模型名称（可选）
# deepseek-v4-flash / deepseek-v4-pro 默认带 thinking；
# 本工具会自动关闭 thinking 并用 JSON 模式做结构化输出。
DEEPSEEK_MODEL=deepseek-v4-flash

# 数据与导出目录（可选，默认为项目根下 prompts/ 与 exports/）
# PROMPT_GEN_DATA_DIR=
# PROMPT_GEN_EXPORT_DIR=
